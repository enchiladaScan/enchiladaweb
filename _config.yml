# --- Identidad del sitio ---
title: "pruebaPAG"
description: "Pagina de pruebas"
# URL pública del usuario y base del proyecto (¡importante para rutas!)
url: "https://zotexd.github.io"
baseurl: "/pruebaPAG.github.io"

# --- Build / Markdown ---
markdown: kramdown
kramdown:
  input: GFM

# Páginas bonitas (/ruta/ en vez de .html)
permalink: pretty

# Tema que está usando Actions
theme: jekyll-theme-primer

# Plugins soportados por GitHub Pages (opcional)
plugins:
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-sitemap

# --- Collections para tus carpetas de series ---
collections:
#activos
  saneka:
    output: true
    permalink: /saneka/:name/
  ririsa:
    output: true
    permalink: /ririsa/:name/
  "100novias":            # clave entre comillas por empezar con número
    output: true
    permalink: /100novias/:name/
  megami:
    output: true
    permalink: /megami-no-cafe/:name/
  dream-jumbo-girl:
    output: true
    permalink: /dream-jumbo/:name/
#joints
  medalist:
    output: true
    permalink: /medalist/:name/
  marchen-crown:
    output: true
    permalink: /marchen-crown/:name/
  kanokari:
    output: true
    permalink: /kanokari/:name/
#terminados
  komi:
    output: true
    permalink: /komi/:name/
  #falta oshi no ko de crear en _posts
  oshi-no-ko:
    output: true
    permalink: /oshi-no-ko/:name/

# --- Defaults: aplica layout caps automáticamente dentro de cada colección ---
defaults:
# activos
  - scope:
      path: "mangas/_saneka"
      type: saneka
    values:
      layout: caps
  - scope:
      path: "mangas/_ririsa"
      type: ririsa
    values:
      layout: caps
  - scope:
      path: "mangas/_100novias"
      type: "100novias"
    values:
      layout: caps
  - scope:
      path: "mangas/_megami"
      type: megami
    values:
      layout: caps
  - scope:
      path: "mangas/_dream-jumbo"
      type: dream
    values:
      layout: caps
# joints
  - scope:
      path: "mangas/_kanokari"
      type: kanokari
    values:
      layout: caps
  - scope:
      path: "mangas/_medalist"
      type: medalist
    values:
      layout: caps
  - scope:
      path: "mangas/_marchen-crown"
      type: marchen-crown
    values:
      layout: caps
# terminados
  - scope:
      path: "mangas/_komi"
      type: komi
    values:
      layout: caps
  - scope:
      path: "mangas/_oshi-no-ko"
      type: oshi-no-ko
    values:
      layout: caps

# --- Archivos/carpetas a excluir del build (se amplía si hace falta) ---
exclude:
  - node_modules
  - vendor
  - README.md
  - checklist.txt
  - guia_rapida.txt

# --- Incluir explícitamente assets si lo necesita ---
include:
  - assets
